{
  "name": "purchase-division-console",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "//": "Scripts call vite through node, not the .bin shim: this repo's path contains an '&', which cmd.exe treats as a command separator and the shim breaks on.",
  "scripts": {
    "dev": "node node_modules/vite/bin/vite.js",
    "build": "node node_modules/vite/bin/vite.js build",
    "preview": "node node_modules/vite/bin/vite.js preview"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.7"
  }
}
